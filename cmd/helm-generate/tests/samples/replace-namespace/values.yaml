# Default values for chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
releaseName: app2
namespace: ns
replicaCount: 1

namespaceOverride: wrong-namespace

image:
  repository: golang
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80